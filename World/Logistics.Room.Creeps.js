var GameTick = require("./Logic.GameTick");

/**
 * Creeps in the room.
 */
module.exports = class Creeps extends GameTick {
    /**
     * Reset the state data.
     */
    reset()
    {
        
    }

    /**
     * Search for creeps.
     */
    getList()
    {
        
    }
}
